// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: verana/poa/v1/query.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import Long = require("long");

export const protobufPackage = "verana.poa.v1";

export interface QueryCouncilRequest {
}

export interface QueryCouncilResponse {
  authority: string;
  groupId: number;
  members: string[];
  membersWithoutValidator: string[];
  validatorsWithoutSeat: string[];
}

function createBaseQueryCouncilRequest(): QueryCouncilRequest {
  return {};
}

export const QueryCouncilRequest = {
  encode(_: QueryCouncilRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryCouncilRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryCouncilRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryCouncilRequest {
    return {};
  },

  toJSON(_: QueryCouncilRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryCouncilRequest>, I>>(base?: I): QueryCouncilRequest {
    return QueryCouncilRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryCouncilRequest>, I>>(_: I): QueryCouncilRequest {
    const message = createBaseQueryCouncilRequest();
    return message;
  },
};

function createBaseQueryCouncilResponse(): QueryCouncilResponse {
  return { authority: "", groupId: 0, members: [], membersWithoutValidator: [], validatorsWithoutSeat: [] };
}

export const QueryCouncilResponse = {
  encode(message: QueryCouncilResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.groupId !== 0) {
      writer.uint32(16).uint64(message.groupId);
    }
    for (const v of message.members) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.membersWithoutValidator) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.validatorsWithoutSeat) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryCouncilResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryCouncilResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.groupId = longToNumber(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.members.push(reader.string());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.membersWithoutValidator.push(reader.string());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.validatorsWithoutSeat.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryCouncilResponse {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      groupId: isSet(object.groupId) ? globalThis.Number(object.groupId) : 0,
      members: globalThis.Array.isArray(object?.members) ? object.members.map((e: any) => globalThis.String(e)) : [],
      membersWithoutValidator: globalThis.Array.isArray(object?.membersWithoutValidator)
        ? object.membersWithoutValidator.map((e: any) => globalThis.String(e))
        : [],
      validatorsWithoutSeat: globalThis.Array.isArray(object?.validatorsWithoutSeat)
        ? object.validatorsWithoutSeat.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: QueryCouncilResponse): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.groupId !== 0) {
      obj.groupId = Math.round(message.groupId);
    }
    if (message.members?.length) {
      obj.members = message.members;
    }
    if (message.membersWithoutValidator?.length) {
      obj.membersWithoutValidator = message.membersWithoutValidator;
    }
    if (message.validatorsWithoutSeat?.length) {
      obj.validatorsWithoutSeat = message.validatorsWithoutSeat;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryCouncilResponse>, I>>(base?: I): QueryCouncilResponse {
    return QueryCouncilResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryCouncilResponse>, I>>(object: I): QueryCouncilResponse {
    const message = createBaseQueryCouncilResponse();
    message.authority = object.authority ?? "";
    message.groupId = object.groupId ?? 0;
    message.members = object.members?.map((e) => e) || [];
    message.membersWithoutValidator = object.membersWithoutValidator?.map((e) => e) || [];
    message.validatorsWithoutSeat = object.validatorsWithoutSeat?.map((e) => e) || [];
    return message;
  },
};

/** Query defines the poa Query service. */
export interface Query {
  /** Council returns the council authority, its members, and members without a bonded validator. */
  Council(request: QueryCouncilRequest): Promise<QueryCouncilResponse>;
}

export const QueryServiceName = "verana.poa.v1.Query";
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName;
    this.rpc = rpc;
    this.Council = this.Council.bind(this);
  }
  Council(request: QueryCouncilRequest): Promise<QueryCouncilResponse> {
    const data = QueryCouncilRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Council", data);
    return promise.then((data) => QueryCouncilResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

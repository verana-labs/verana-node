// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: verana/co/v1/query.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { Params } from "./params";
import { CorporationWithGF } from "./types";
import Long = require("long");

export const protobufPackage = "verana.co.v1";

export interface QueryParamsRequest {
}

export interface QueryParamsResponse {
  params: Params | undefined;
}

export interface QueryGetCorporationRequest {
  corporationId: number;
  activeGfOnly: boolean;
  preferredLanguage: string;
}

export interface QueryGetCorporationResponse {
  corporation: CorporationWithGF | undefined;
}

export interface QueryListCorporationsRequest {
  modifiedAfter: Date | undefined;
  activeGfOnly: boolean;
  preferredLanguage: string;
  responseMaxSize: number;
}

export interface QueryListCorporationsResponse {
  corporations: CorporationWithGF[];
}

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest = {
  encode(_: QueryParamsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(base?: I): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(_: I): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse = {
  encode(message: QueryParamsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(base?: I): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(object: I): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryGetCorporationRequest(): QueryGetCorporationRequest {
  return { corporationId: 0, activeGfOnly: false, preferredLanguage: "" };
}

export const QueryGetCorporationRequest = {
  encode(message: QueryGetCorporationRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.corporationId !== 0) {
      writer.uint32(8).uint64(message.corporationId);
    }
    if (message.activeGfOnly !== false) {
      writer.uint32(16).bool(message.activeGfOnly);
    }
    if (message.preferredLanguage !== "") {
      writer.uint32(26).string(message.preferredLanguage);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryGetCorporationRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetCorporationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.corporationId = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.activeGfOnly = reader.bool();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.preferredLanguage = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetCorporationRequest {
    return {
      corporationId: isSet(object.corporationId) ? globalThis.Number(object.corporationId) : 0,
      activeGfOnly: isSet(object.activeGfOnly) ? globalThis.Boolean(object.activeGfOnly) : false,
      preferredLanguage: isSet(object.preferredLanguage) ? globalThis.String(object.preferredLanguage) : "",
    };
  },

  toJSON(message: QueryGetCorporationRequest): unknown {
    const obj: any = {};
    if (message.corporationId !== 0) {
      obj.corporationId = Math.round(message.corporationId);
    }
    if (message.activeGfOnly !== false) {
      obj.activeGfOnly = message.activeGfOnly;
    }
    if (message.preferredLanguage !== "") {
      obj.preferredLanguage = message.preferredLanguage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetCorporationRequest>, I>>(base?: I): QueryGetCorporationRequest {
    return QueryGetCorporationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetCorporationRequest>, I>>(object: I): QueryGetCorporationRequest {
    const message = createBaseQueryGetCorporationRequest();
    message.corporationId = object.corporationId ?? 0;
    message.activeGfOnly = object.activeGfOnly ?? false;
    message.preferredLanguage = object.preferredLanguage ?? "";
    return message;
  },
};

function createBaseQueryGetCorporationResponse(): QueryGetCorporationResponse {
  return { corporation: undefined };
}

export const QueryGetCorporationResponse = {
  encode(message: QueryGetCorporationResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.corporation !== undefined) {
      CorporationWithGF.encode(message.corporation, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryGetCorporationResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetCorporationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.corporation = CorporationWithGF.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetCorporationResponse {
    return { corporation: isSet(object.corporation) ? CorporationWithGF.fromJSON(object.corporation) : undefined };
  },

  toJSON(message: QueryGetCorporationResponse): unknown {
    const obj: any = {};
    if (message.corporation !== undefined) {
      obj.corporation = CorporationWithGF.toJSON(message.corporation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetCorporationResponse>, I>>(base?: I): QueryGetCorporationResponse {
    return QueryGetCorporationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetCorporationResponse>, I>>(object: I): QueryGetCorporationResponse {
    const message = createBaseQueryGetCorporationResponse();
    message.corporation = (object.corporation !== undefined && object.corporation !== null)
      ? CorporationWithGF.fromPartial(object.corporation)
      : undefined;
    return message;
  },
};

function createBaseQueryListCorporationsRequest(): QueryListCorporationsRequest {
  return { modifiedAfter: undefined, activeGfOnly: false, preferredLanguage: "", responseMaxSize: 0 };
}

export const QueryListCorporationsRequest = {
  encode(message: QueryListCorporationsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.modifiedAfter !== undefined) {
      Timestamp.encode(toTimestamp(message.modifiedAfter), writer.uint32(10).fork()).ldelim();
    }
    if (message.activeGfOnly !== false) {
      writer.uint32(16).bool(message.activeGfOnly);
    }
    if (message.preferredLanguage !== "") {
      writer.uint32(26).string(message.preferredLanguage);
    }
    if (message.responseMaxSize !== 0) {
      writer.uint32(32).uint32(message.responseMaxSize);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryListCorporationsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListCorporationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.modifiedAfter = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.activeGfOnly = reader.bool();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.preferredLanguage = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.responseMaxSize = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListCorporationsRequest {
    return {
      modifiedAfter: isSet(object.modifiedAfter) ? fromJsonTimestamp(object.modifiedAfter) : undefined,
      activeGfOnly: isSet(object.activeGfOnly) ? globalThis.Boolean(object.activeGfOnly) : false,
      preferredLanguage: isSet(object.preferredLanguage) ? globalThis.String(object.preferredLanguage) : "",
      responseMaxSize: isSet(object.responseMaxSize) ? globalThis.Number(object.responseMaxSize) : 0,
    };
  },

  toJSON(message: QueryListCorporationsRequest): unknown {
    const obj: any = {};
    if (message.modifiedAfter !== undefined) {
      obj.modifiedAfter = message.modifiedAfter.toISOString();
    }
    if (message.activeGfOnly !== false) {
      obj.activeGfOnly = message.activeGfOnly;
    }
    if (message.preferredLanguage !== "") {
      obj.preferredLanguage = message.preferredLanguage;
    }
    if (message.responseMaxSize !== 0) {
      obj.responseMaxSize = Math.round(message.responseMaxSize);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryListCorporationsRequest>, I>>(base?: I): QueryListCorporationsRequest {
    return QueryListCorporationsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryListCorporationsRequest>, I>>(object: I): QueryListCorporationsRequest {
    const message = createBaseQueryListCorporationsRequest();
    message.modifiedAfter = object.modifiedAfter ?? undefined;
    message.activeGfOnly = object.activeGfOnly ?? false;
    message.preferredLanguage = object.preferredLanguage ?? "";
    message.responseMaxSize = object.responseMaxSize ?? 0;
    return message;
  },
};

function createBaseQueryListCorporationsResponse(): QueryListCorporationsResponse {
  return { corporations: [] };
}

export const QueryListCorporationsResponse = {
  encode(message: QueryListCorporationsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.corporations) {
      CorporationWithGF.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryListCorporationsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListCorporationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.corporations.push(CorporationWithGF.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListCorporationsResponse {
    return {
      corporations: globalThis.Array.isArray(object?.corporations)
        ? object.corporations.map((e: any) => CorporationWithGF.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryListCorporationsResponse): unknown {
    const obj: any = {};
    if (message.corporations?.length) {
      obj.corporations = message.corporations.map((e) => CorporationWithGF.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryListCorporationsResponse>, I>>(base?: I): QueryListCorporationsResponse {
    return QueryListCorporationsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryListCorporationsResponse>, I>>(
    object: I,
  ): QueryListCorporationsResponse {
    const message = createBaseQueryListCorporationsResponse();
    message.corporations = object.corporations?.map((e) => CorporationWithGF.fromPartial(e)) || [];
    return message;
  },
};

/** Query defines the Query service. */
export interface Query {
  /** Params returns the total set of module parameters. */
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse>;
  /** [MOD-CO-QRY-1] Get Corporation by id, with nested GF version + document data. */
  GetCorporation(request: QueryGetCorporationRequest): Promise<QueryGetCorporationResponse>;
  /**
   * [MOD-CO-QRY-2] List Corporations, optionally filtered by modified_after,
   * with nested GF data per corporation. Ordered by `modified` descending.
   */
  ListCorporations(request: QueryListCorporationsRequest): Promise<QueryListCorporationsResponse>;
}

export const QueryServiceName = "verana.co.v1.Query";
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName;
    this.rpc = rpc;
    this.Params = this.Params.bind(this);
    this.GetCorporation = this.GetCorporation.bind(this);
    this.ListCorporations = this.ListCorporations.bind(this);
  }
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse> {
    const data = QueryParamsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Params", data);
    return promise.then((data) => QueryParamsResponse.decode(_m0.Reader.create(data)));
  }

  GetCorporation(request: QueryGetCorporationRequest): Promise<QueryGetCorporationResponse> {
    const data = QueryGetCorporationRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetCorporation", data);
    return promise.then((data) => QueryGetCorporationResponse.decode(_m0.Reader.create(data)));
  }

  ListCorporations(request: QueryListCorporationsRequest): Promise<QueryListCorporationsResponse> {
    const data = QueryListCorporationsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListCorporations", data);
    return promise.then((data) => QueryListCorporationsResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

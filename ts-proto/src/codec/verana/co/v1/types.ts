// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: verana/co/v1/types.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { GovernanceFrameworkVersionWithDocs } from "../../gf/v1/types";
import Long = require("long");

export const protobufPackage = "verana.co.v1";

/**
 * Corporation is the VPR-level entity that represents a legal/organizational
 * authority acting in the registry. Identified by its uint64 id everywhere
 * dependent entities reference it; anchored on-chain by a policy_address
 * account that signs Msgs on its behalf.
 */
export interface Corporation {
  id: number;
  policyAddress: string;
  did: string;
  created: Date | undefined;
  modified: Date | undefined;
  language: string;
  activeVersion: number;
}

/**
 * CorporationWithGF is the query response shape including nested governance
 * framework versions (each with its documents). Returned by MOD-CO-QRY-1
 * (Get Corporation) and MOD-CO-QRY-2 (List Corporations).
 */
export interface CorporationWithGF {
  id: number;
  policyAddress: string;
  did: string;
  created: Date | undefined;
  modified: Date | undefined;
  language: string;
  activeVersion: number;
  versions: GovernanceFrameworkVersionWithDocs[];
}

function createBaseCorporation(): Corporation {
  return { id: 0, policyAddress: "", did: "", created: undefined, modified: undefined, language: "", activeVersion: 0 };
}

export const Corporation = {
  encode(message: Corporation, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.policyAddress !== "") {
      writer.uint32(18).string(message.policyAddress);
    }
    if (message.did !== "") {
      writer.uint32(26).string(message.did);
    }
    if (message.created !== undefined) {
      Timestamp.encode(toTimestamp(message.created), writer.uint32(34).fork()).ldelim();
    }
    if (message.modified !== undefined) {
      Timestamp.encode(toTimestamp(message.modified), writer.uint32(42).fork()).ldelim();
    }
    if (message.language !== "") {
      writer.uint32(50).string(message.language);
    }
    if (message.activeVersion !== 0) {
      writer.uint32(56).uint32(message.activeVersion);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Corporation {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCorporation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.policyAddress = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.did = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.created = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.modified = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.language = reader.string();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.activeVersion = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Corporation {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      policyAddress: isSet(object.policyAddress) ? globalThis.String(object.policyAddress) : "",
      did: isSet(object.did) ? globalThis.String(object.did) : "",
      created: isSet(object.created) ? fromJsonTimestamp(object.created) : undefined,
      modified: isSet(object.modified) ? fromJsonTimestamp(object.modified) : undefined,
      language: isSet(object.language) ? globalThis.String(object.language) : "",
      activeVersion: isSet(object.activeVersion) ? globalThis.Number(object.activeVersion) : 0,
    };
  },

  toJSON(message: Corporation): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.policyAddress !== "") {
      obj.policyAddress = message.policyAddress;
    }
    if (message.did !== "") {
      obj.did = message.did;
    }
    if (message.created !== undefined) {
      obj.created = message.created.toISOString();
    }
    if (message.modified !== undefined) {
      obj.modified = message.modified.toISOString();
    }
    if (message.language !== "") {
      obj.language = message.language;
    }
    if (message.activeVersion !== 0) {
      obj.activeVersion = Math.round(message.activeVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Corporation>, I>>(base?: I): Corporation {
    return Corporation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Corporation>, I>>(object: I): Corporation {
    const message = createBaseCorporation();
    message.id = object.id ?? 0;
    message.policyAddress = object.policyAddress ?? "";
    message.did = object.did ?? "";
    message.created = object.created ?? undefined;
    message.modified = object.modified ?? undefined;
    message.language = object.language ?? "";
    message.activeVersion = object.activeVersion ?? 0;
    return message;
  },
};

function createBaseCorporationWithGF(): CorporationWithGF {
  return {
    id: 0,
    policyAddress: "",
    did: "",
    created: undefined,
    modified: undefined,
    language: "",
    activeVersion: 0,
    versions: [],
  };
}

export const CorporationWithGF = {
  encode(message: CorporationWithGF, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.policyAddress !== "") {
      writer.uint32(18).string(message.policyAddress);
    }
    if (message.did !== "") {
      writer.uint32(26).string(message.did);
    }
    if (message.created !== undefined) {
      Timestamp.encode(toTimestamp(message.created), writer.uint32(34).fork()).ldelim();
    }
    if (message.modified !== undefined) {
      Timestamp.encode(toTimestamp(message.modified), writer.uint32(42).fork()).ldelim();
    }
    if (message.language !== "") {
      writer.uint32(50).string(message.language);
    }
    if (message.activeVersion !== 0) {
      writer.uint32(56).uint32(message.activeVersion);
    }
    for (const v of message.versions) {
      GovernanceFrameworkVersionWithDocs.encode(v!, writer.uint32(66).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CorporationWithGF {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCorporationWithGF();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.policyAddress = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.did = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.created = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.modified = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.language = reader.string();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.activeVersion = reader.uint32();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.versions.push(GovernanceFrameworkVersionWithDocs.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CorporationWithGF {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      policyAddress: isSet(object.policyAddress) ? globalThis.String(object.policyAddress) : "",
      did: isSet(object.did) ? globalThis.String(object.did) : "",
      created: isSet(object.created) ? fromJsonTimestamp(object.created) : undefined,
      modified: isSet(object.modified) ? fromJsonTimestamp(object.modified) : undefined,
      language: isSet(object.language) ? globalThis.String(object.language) : "",
      activeVersion: isSet(object.activeVersion) ? globalThis.Number(object.activeVersion) : 0,
      versions: globalThis.Array.isArray(object?.versions)
        ? object.versions.map((e: any) => GovernanceFrameworkVersionWithDocs.fromJSON(e))
        : [],
    };
  },

  toJSON(message: CorporationWithGF): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.policyAddress !== "") {
      obj.policyAddress = message.policyAddress;
    }
    if (message.did !== "") {
      obj.did = message.did;
    }
    if (message.created !== undefined) {
      obj.created = message.created.toISOString();
    }
    if (message.modified !== undefined) {
      obj.modified = message.modified.toISOString();
    }
    if (message.language !== "") {
      obj.language = message.language;
    }
    if (message.activeVersion !== 0) {
      obj.activeVersion = Math.round(message.activeVersion);
    }
    if (message.versions?.length) {
      obj.versions = message.versions.map((e) => GovernanceFrameworkVersionWithDocs.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CorporationWithGF>, I>>(base?: I): CorporationWithGF {
    return CorporationWithGF.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CorporationWithGF>, I>>(object: I): CorporationWithGF {
    const message = createBaseCorporationWithGF();
    message.id = object.id ?? 0;
    message.policyAddress = object.policyAddress ?? "";
    message.did = object.did ?? "";
    message.created = object.created ?? undefined;
    message.modified = object.modified ?? undefined;
    message.language = object.language ?? "";
    message.activeVersion = object.activeVersion ?? 0;
    message.versions = object.versions?.map((e) => GovernanceFrameworkVersionWithDocs.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: verana/pp/v1/query.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { Params } from "./params";
import {
  OnboardingState,
  onboardingStateFromJSON,
  onboardingStateToJSON,
  Participant,
  ParticipantRole,
  participantRoleFromJSON,
  participantRoleToJSON,
  ParticipantSession,
} from "./types";
import Long = require("long");

export const protobufPackage = "verana.pp.v1";

/** QueryParamsRequest is request type for the Query/Params RPC method. */
export interface QueryParamsRequest {
}

/** QueryParamsResponse is response type for the Query/Params RPC method. */
export interface QueryParamsResponse {
  /** params holds all the parameters of this module. */
  params: Params | undefined;
}

export interface QueryListParticipantsRequest {
  modifiedAfter:
    | Date
    | undefined;
  /** Default 64, min 1, max 1024 */
  responseMaxSize: number;
  schemaId: number;
  /** filters on Participant.vs_operator (the VS grantee service account) */
  grantee: string;
  did: string;
  /** matches Participant.validator_participant_id */
  participantId: number;
  role: ParticipantRole;
  onlyValid: boolean;
  onlySlashed: boolean;
  onlyRepaid: boolean;
  opState: OnboardingState;
  when: Date | undefined;
}

export interface QueryListParticipantsResponse {
  participants: Participant[];
}

export interface QueryGetParticipantRequest {
  id: number;
}

export interface QueryGetParticipantResponse {
  participant: Participant | undefined;
}

export interface QueryGetParticipantSessionRequest {
  /** UUID */
  id: string;
}

export interface QueryGetParticipantSessionResponse {
  session: ParticipantSession | undefined;
}

export interface QueryListParticipantSessionsRequest {
  modifiedAfter: Date | undefined;
  responseMaxSize: number;
}

export interface QueryListParticipantSessionsResponse {
  sessions: ParticipantSession[];
}

export interface QueryFindBeneficiariesRequest {
  issuerParticipantId: number;
  verifierParticipantId: number;
}

export interface QueryFindBeneficiariesResponse {
  participants: Participant[];
}

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest = {
  encode(_: QueryParamsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(base?: I): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(_: I): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse = {
  encode(message: QueryParamsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(base?: I): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(object: I): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryListParticipantsRequest(): QueryListParticipantsRequest {
  return {
    modifiedAfter: undefined,
    responseMaxSize: 0,
    schemaId: 0,
    grantee: "",
    did: "",
    participantId: 0,
    role: 0,
    onlyValid: false,
    onlySlashed: false,
    onlyRepaid: false,
    opState: 0,
    when: undefined,
  };
}

export const QueryListParticipantsRequest = {
  encode(message: QueryListParticipantsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.modifiedAfter !== undefined) {
      Timestamp.encode(toTimestamp(message.modifiedAfter), writer.uint32(10).fork()).ldelim();
    }
    if (message.responseMaxSize !== 0) {
      writer.uint32(16).uint32(message.responseMaxSize);
    }
    if (message.schemaId !== 0) {
      writer.uint32(24).uint64(message.schemaId);
    }
    if (message.grantee !== "") {
      writer.uint32(34).string(message.grantee);
    }
    if (message.did !== "") {
      writer.uint32(42).string(message.did);
    }
    if (message.participantId !== 0) {
      writer.uint32(48).uint64(message.participantId);
    }
    if (message.role !== 0) {
      writer.uint32(56).int32(message.role);
    }
    if (message.onlyValid !== false) {
      writer.uint32(64).bool(message.onlyValid);
    }
    if (message.onlySlashed !== false) {
      writer.uint32(72).bool(message.onlySlashed);
    }
    if (message.onlyRepaid !== false) {
      writer.uint32(80).bool(message.onlyRepaid);
    }
    if (message.opState !== 0) {
      writer.uint32(88).int32(message.opState);
    }
    if (message.when !== undefined) {
      Timestamp.encode(toTimestamp(message.when), writer.uint32(98).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryListParticipantsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListParticipantsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.modifiedAfter = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.responseMaxSize = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.schemaId = longToNumber(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.grantee = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.did = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.participantId = longToNumber(reader.uint64() as Long);
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.onlyValid = reader.bool();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.onlySlashed = reader.bool();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.onlyRepaid = reader.bool();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.opState = reader.int32() as any;
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.when = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListParticipantsRequest {
    return {
      modifiedAfter: isSet(object.modifiedAfter) ? fromJsonTimestamp(object.modifiedAfter) : undefined,
      responseMaxSize: isSet(object.responseMaxSize) ? globalThis.Number(object.responseMaxSize) : 0,
      schemaId: isSet(object.schemaId) ? globalThis.Number(object.schemaId) : 0,
      grantee: isSet(object.grantee) ? globalThis.String(object.grantee) : "",
      did: isSet(object.did) ? globalThis.String(object.did) : "",
      participantId: isSet(object.participantId) ? globalThis.Number(object.participantId) : 0,
      role: isSet(object.role) ? participantRoleFromJSON(object.role) : 0,
      onlyValid: isSet(object.onlyValid) ? globalThis.Boolean(object.onlyValid) : false,
      onlySlashed: isSet(object.onlySlashed) ? globalThis.Boolean(object.onlySlashed) : false,
      onlyRepaid: isSet(object.onlyRepaid) ? globalThis.Boolean(object.onlyRepaid) : false,
      opState: isSet(object.opState) ? onboardingStateFromJSON(object.opState) : 0,
      when: isSet(object.when) ? fromJsonTimestamp(object.when) : undefined,
    };
  },

  toJSON(message: QueryListParticipantsRequest): unknown {
    const obj: any = {};
    if (message.modifiedAfter !== undefined) {
      obj.modifiedAfter = message.modifiedAfter.toISOString();
    }
    if (message.responseMaxSize !== 0) {
      obj.responseMaxSize = Math.round(message.responseMaxSize);
    }
    if (message.schemaId !== 0) {
      obj.schemaId = Math.round(message.schemaId);
    }
    if (message.grantee !== "") {
      obj.grantee = message.grantee;
    }
    if (message.did !== "") {
      obj.did = message.did;
    }
    if (message.participantId !== 0) {
      obj.participantId = Math.round(message.participantId);
    }
    if (message.role !== 0) {
      obj.role = participantRoleToJSON(message.role);
    }
    if (message.onlyValid !== false) {
      obj.onlyValid = message.onlyValid;
    }
    if (message.onlySlashed !== false) {
      obj.onlySlashed = message.onlySlashed;
    }
    if (message.onlyRepaid !== false) {
      obj.onlyRepaid = message.onlyRepaid;
    }
    if (message.opState !== 0) {
      obj.opState = onboardingStateToJSON(message.opState);
    }
    if (message.when !== undefined) {
      obj.when = message.when.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryListParticipantsRequest>, I>>(base?: I): QueryListParticipantsRequest {
    return QueryListParticipantsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryListParticipantsRequest>, I>>(object: I): QueryListParticipantsRequest {
    const message = createBaseQueryListParticipantsRequest();
    message.modifiedAfter = object.modifiedAfter ?? undefined;
    message.responseMaxSize = object.responseMaxSize ?? 0;
    message.schemaId = object.schemaId ?? 0;
    message.grantee = object.grantee ?? "";
    message.did = object.did ?? "";
    message.participantId = object.participantId ?? 0;
    message.role = object.role ?? 0;
    message.onlyValid = object.onlyValid ?? false;
    message.onlySlashed = object.onlySlashed ?? false;
    message.onlyRepaid = object.onlyRepaid ?? false;
    message.opState = object.opState ?? 0;
    message.when = object.when ?? undefined;
    return message;
  },
};

function createBaseQueryListParticipantsResponse(): QueryListParticipantsResponse {
  return { participants: [] };
}

export const QueryListParticipantsResponse = {
  encode(message: QueryListParticipantsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.participants) {
      Participant.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryListParticipantsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListParticipantsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.participants.push(Participant.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListParticipantsResponse {
    return {
      participants: globalThis.Array.isArray(object?.participants)
        ? object.participants.map((e: any) => Participant.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryListParticipantsResponse): unknown {
    const obj: any = {};
    if (message.participants?.length) {
      obj.participants = message.participants.map((e) => Participant.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryListParticipantsResponse>, I>>(base?: I): QueryListParticipantsResponse {
    return QueryListParticipantsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryListParticipantsResponse>, I>>(
    object: I,
  ): QueryListParticipantsResponse {
    const message = createBaseQueryListParticipantsResponse();
    message.participants = object.participants?.map((e) => Participant.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryGetParticipantRequest(): QueryGetParticipantRequest {
  return { id: 0 };
}

export const QueryGetParticipantRequest = {
  encode(message: QueryGetParticipantRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryGetParticipantRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetParticipantRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetParticipantRequest {
    return { id: isSet(object.id) ? globalThis.Number(object.id) : 0 };
  },

  toJSON(message: QueryGetParticipantRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetParticipantRequest>, I>>(base?: I): QueryGetParticipantRequest {
    return QueryGetParticipantRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetParticipantRequest>, I>>(object: I): QueryGetParticipantRequest {
    const message = createBaseQueryGetParticipantRequest();
    message.id = object.id ?? 0;
    return message;
  },
};

function createBaseQueryGetParticipantResponse(): QueryGetParticipantResponse {
  return { participant: undefined };
}

export const QueryGetParticipantResponse = {
  encode(message: QueryGetParticipantResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.participant !== undefined) {
      Participant.encode(message.participant, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryGetParticipantResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetParticipantResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.participant = Participant.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetParticipantResponse {
    return { participant: isSet(object.participant) ? Participant.fromJSON(object.participant) : undefined };
  },

  toJSON(message: QueryGetParticipantResponse): unknown {
    const obj: any = {};
    if (message.participant !== undefined) {
      obj.participant = Participant.toJSON(message.participant);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetParticipantResponse>, I>>(base?: I): QueryGetParticipantResponse {
    return QueryGetParticipantResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetParticipantResponse>, I>>(object: I): QueryGetParticipantResponse {
    const message = createBaseQueryGetParticipantResponse();
    message.participant = (object.participant !== undefined && object.participant !== null)
      ? Participant.fromPartial(object.participant)
      : undefined;
    return message;
  },
};

function createBaseQueryGetParticipantSessionRequest(): QueryGetParticipantSessionRequest {
  return { id: "" };
}

export const QueryGetParticipantSessionRequest = {
  encode(message: QueryGetParticipantSessionRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryGetParticipantSessionRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetParticipantSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetParticipantSessionRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: QueryGetParticipantSessionRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetParticipantSessionRequest>, I>>(
    base?: I,
  ): QueryGetParticipantSessionRequest {
    return QueryGetParticipantSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetParticipantSessionRequest>, I>>(
    object: I,
  ): QueryGetParticipantSessionRequest {
    const message = createBaseQueryGetParticipantSessionRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseQueryGetParticipantSessionResponse(): QueryGetParticipantSessionResponse {
  return { session: undefined };
}

export const QueryGetParticipantSessionResponse = {
  encode(message: QueryGetParticipantSessionResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.session !== undefined) {
      ParticipantSession.encode(message.session, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryGetParticipantSessionResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryGetParticipantSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.session = ParticipantSession.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryGetParticipantSessionResponse {
    return { session: isSet(object.session) ? ParticipantSession.fromJSON(object.session) : undefined };
  },

  toJSON(message: QueryGetParticipantSessionResponse): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = ParticipantSession.toJSON(message.session);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryGetParticipantSessionResponse>, I>>(
    base?: I,
  ): QueryGetParticipantSessionResponse {
    return QueryGetParticipantSessionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryGetParticipantSessionResponse>, I>>(
    object: I,
  ): QueryGetParticipantSessionResponse {
    const message = createBaseQueryGetParticipantSessionResponse();
    message.session = (object.session !== undefined && object.session !== null)
      ? ParticipantSession.fromPartial(object.session)
      : undefined;
    return message;
  },
};

function createBaseQueryListParticipantSessionsRequest(): QueryListParticipantSessionsRequest {
  return { modifiedAfter: undefined, responseMaxSize: 0 };
}

export const QueryListParticipantSessionsRequest = {
  encode(message: QueryListParticipantSessionsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.modifiedAfter !== undefined) {
      Timestamp.encode(toTimestamp(message.modifiedAfter), writer.uint32(10).fork()).ldelim();
    }
    if (message.responseMaxSize !== 0) {
      writer.uint32(16).uint32(message.responseMaxSize);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryListParticipantSessionsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListParticipantSessionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.modifiedAfter = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.responseMaxSize = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListParticipantSessionsRequest {
    return {
      modifiedAfter: isSet(object.modifiedAfter) ? fromJsonTimestamp(object.modifiedAfter) : undefined,
      responseMaxSize: isSet(object.responseMaxSize) ? globalThis.Number(object.responseMaxSize) : 0,
    };
  },

  toJSON(message: QueryListParticipantSessionsRequest): unknown {
    const obj: any = {};
    if (message.modifiedAfter !== undefined) {
      obj.modifiedAfter = message.modifiedAfter.toISOString();
    }
    if (message.responseMaxSize !== 0) {
      obj.responseMaxSize = Math.round(message.responseMaxSize);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryListParticipantSessionsRequest>, I>>(
    base?: I,
  ): QueryListParticipantSessionsRequest {
    return QueryListParticipantSessionsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryListParticipantSessionsRequest>, I>>(
    object: I,
  ): QueryListParticipantSessionsRequest {
    const message = createBaseQueryListParticipantSessionsRequest();
    message.modifiedAfter = object.modifiedAfter ?? undefined;
    message.responseMaxSize = object.responseMaxSize ?? 0;
    return message;
  },
};

function createBaseQueryListParticipantSessionsResponse(): QueryListParticipantSessionsResponse {
  return { sessions: [] };
}

export const QueryListParticipantSessionsResponse = {
  encode(message: QueryListParticipantSessionsResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.sessions) {
      ParticipantSession.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryListParticipantSessionsResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryListParticipantSessionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.sessions.push(ParticipantSession.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryListParticipantSessionsResponse {
    return {
      sessions: globalThis.Array.isArray(object?.sessions)
        ? object.sessions.map((e: any) => ParticipantSession.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryListParticipantSessionsResponse): unknown {
    const obj: any = {};
    if (message.sessions?.length) {
      obj.sessions = message.sessions.map((e) => ParticipantSession.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryListParticipantSessionsResponse>, I>>(
    base?: I,
  ): QueryListParticipantSessionsResponse {
    return QueryListParticipantSessionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryListParticipantSessionsResponse>, I>>(
    object: I,
  ): QueryListParticipantSessionsResponse {
    const message = createBaseQueryListParticipantSessionsResponse();
    message.sessions = object.sessions?.map((e) => ParticipantSession.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryFindBeneficiariesRequest(): QueryFindBeneficiariesRequest {
  return { issuerParticipantId: 0, verifierParticipantId: 0 };
}

export const QueryFindBeneficiariesRequest = {
  encode(message: QueryFindBeneficiariesRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.issuerParticipantId !== 0) {
      writer.uint32(8).uint64(message.issuerParticipantId);
    }
    if (message.verifierParticipantId !== 0) {
      writer.uint32(16).uint64(message.verifierParticipantId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryFindBeneficiariesRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryFindBeneficiariesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.issuerParticipantId = longToNumber(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.verifierParticipantId = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryFindBeneficiariesRequest {
    return {
      issuerParticipantId: isSet(object.issuerParticipantId) ? globalThis.Number(object.issuerParticipantId) : 0,
      verifierParticipantId: isSet(object.verifierParticipantId) ? globalThis.Number(object.verifierParticipantId) : 0,
    };
  },

  toJSON(message: QueryFindBeneficiariesRequest): unknown {
    const obj: any = {};
    if (message.issuerParticipantId !== 0) {
      obj.issuerParticipantId = Math.round(message.issuerParticipantId);
    }
    if (message.verifierParticipantId !== 0) {
      obj.verifierParticipantId = Math.round(message.verifierParticipantId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryFindBeneficiariesRequest>, I>>(base?: I): QueryFindBeneficiariesRequest {
    return QueryFindBeneficiariesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryFindBeneficiariesRequest>, I>>(
    object: I,
  ): QueryFindBeneficiariesRequest {
    const message = createBaseQueryFindBeneficiariesRequest();
    message.issuerParticipantId = object.issuerParticipantId ?? 0;
    message.verifierParticipantId = object.verifierParticipantId ?? 0;
    return message;
  },
};

function createBaseQueryFindBeneficiariesResponse(): QueryFindBeneficiariesResponse {
  return { participants: [] };
}

export const QueryFindBeneficiariesResponse = {
  encode(message: QueryFindBeneficiariesResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.participants) {
      Participant.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryFindBeneficiariesResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryFindBeneficiariesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.participants.push(Participant.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryFindBeneficiariesResponse {
    return {
      participants: globalThis.Array.isArray(object?.participants)
        ? object.participants.map((e: any) => Participant.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryFindBeneficiariesResponse): unknown {
    const obj: any = {};
    if (message.participants?.length) {
      obj.participants = message.participants.map((e) => Participant.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryFindBeneficiariesResponse>, I>>(base?: I): QueryFindBeneficiariesResponse {
    return QueryFindBeneficiariesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryFindBeneficiariesResponse>, I>>(
    object: I,
  ): QueryFindBeneficiariesResponse {
    const message = createBaseQueryFindBeneficiariesResponse();
    message.participants = object.participants?.map((e) => Participant.fromPartial(e)) || [];
    return message;
  },
};

/** Query defines the gRPC querier service. */
export interface Query {
  /** Parameters queries the parameters of the module. */
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse>;
  ListParticipants(request: QueryListParticipantsRequest): Promise<QueryListParticipantsResponse>;
  GetParticipant(request: QueryGetParticipantRequest): Promise<QueryGetParticipantResponse>;
  GetParticipantSession(request: QueryGetParticipantSessionRequest): Promise<QueryGetParticipantSessionResponse>;
  ListParticipantSessions(request: QueryListParticipantSessionsRequest): Promise<QueryListParticipantSessionsResponse>;
  FindBeneficiaries(request: QueryFindBeneficiariesRequest): Promise<QueryFindBeneficiariesResponse>;
}

export const QueryServiceName = "verana.pp.v1.Query";
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName;
    this.rpc = rpc;
    this.Params = this.Params.bind(this);
    this.ListParticipants = this.ListParticipants.bind(this);
    this.GetParticipant = this.GetParticipant.bind(this);
    this.GetParticipantSession = this.GetParticipantSession.bind(this);
    this.ListParticipantSessions = this.ListParticipantSessions.bind(this);
    this.FindBeneficiaries = this.FindBeneficiaries.bind(this);
  }
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse> {
    const data = QueryParamsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Params", data);
    return promise.then((data) => QueryParamsResponse.decode(_m0.Reader.create(data)));
  }

  ListParticipants(request: QueryListParticipantsRequest): Promise<QueryListParticipantsResponse> {
    const data = QueryListParticipantsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListParticipants", data);
    return promise.then((data) => QueryListParticipantsResponse.decode(_m0.Reader.create(data)));
  }

  GetParticipant(request: QueryGetParticipantRequest): Promise<QueryGetParticipantResponse> {
    const data = QueryGetParticipantRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetParticipant", data);
    return promise.then((data) => QueryGetParticipantResponse.decode(_m0.Reader.create(data)));
  }

  GetParticipantSession(request: QueryGetParticipantSessionRequest): Promise<QueryGetParticipantSessionResponse> {
    const data = QueryGetParticipantSessionRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetParticipantSession", data);
    return promise.then((data) => QueryGetParticipantSessionResponse.decode(_m0.Reader.create(data)));
  }

  ListParticipantSessions(request: QueryListParticipantSessionsRequest): Promise<QueryListParticipantSessionsResponse> {
    const data = QueryListParticipantSessionsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ListParticipantSessions", data);
    return promise.then((data) => QueryListParticipantSessionsResponse.decode(_m0.Reader.create(data)));
  }

  FindBeneficiaries(request: QueryFindBeneficiariesRequest): Promise<QueryFindBeneficiariesResponse> {
    const data = QueryFindBeneficiariesRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "FindBeneficiaries", data);
    return promise.then((data) => QueryFindBeneficiariesResponse.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
